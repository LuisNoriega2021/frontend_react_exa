//import { dbConnection } from '../../src/db/connection';
//import { EnvironmentServer } from '../../src/configs/EnvironmentServer';

describe('Base de datos', () => {
	/*
	it('Conexión en línea', (done) => {
		dbConnection(EnvironmentServer.ConnectionMongoDD)
			.then(() => done());
	});

	it('Conexión fallida', (done) => {
		dbConnection('conexion.inventada')
			.catch(() => done());
	});*/
});
